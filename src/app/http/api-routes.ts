export enum ApiRoutes {
  // Auth
  REGISTER = '/registration',
  LOGIN = '/login',
  LOGOUT = '/logout',
  REFRESH = '/refresh',
  REMEMBER = '/remember',
  REDACT = '/redact',
  // Users
  USERS = '/users'
}
